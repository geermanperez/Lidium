# =============================================================================
# LatinMS v111 (Lidium) — docker-compose.local.yml
# Override para desarrollo local: expone MySQL en puerto 3307 del host.
#
# Uso:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up
#
# ADVERTENCIA: No usar este override en produccion.
# =============================================================================

services:
  db:
    ports:
      - "3307:3306"
